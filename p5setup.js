function setup() {
  let myCanvas = createCanvas(760, 400);
  frameRate(30);
}

window.addEventListener("resize", function () {
  resizeCanvas(760, 400);
  clear();
});

new p5();
var width = 760;
var height = 400;
